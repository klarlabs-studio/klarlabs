{
  "name": "klarlabs",
  "private": true,
  "version": "0.1.0",
  "description": "Klarlabs — precision software from Munich. Monorepo for the studio design system and sites.",
  "type": "module",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "dev": "pnpm --filter @klarlabs/ui dev"
  },
  "engines": {
    "node": ">=20"
  },
  "packageManager": "pnpm@10.24.0",
  "dependencies": {
    "brace-expansion": "^2.1.2",
    "esbuild": "^0.28.1",
    "fast-uri": "^3.1.4",
    "js-yaml": "^4.3.0",
    "postcss": "^8.5.18",
    "sharp": "^0.35.0",
    "svgo": "^4.0.2",
    "yaml": "^2.8.3"
  }
}
